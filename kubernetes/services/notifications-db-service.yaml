apiVersion: v1
kind: Service
metadata:
  name: notifications-db
  labels:
    app: notifications-db
spec:
  clusterIP: None
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
  selector:
    app: notifications-db
