apiVersion: v1
kind: Service
metadata:
  name: shipments-service
  labels:
    app: shipments-service
spec:
  type: ClusterIP
  ports:
  - port: 3003
    targetPort: 3003
    protocol: TCP
  selector:
    app: shipments-service
