apiVersion: v1
kind: Service
metadata:
  name: users-db
  labels:
    app: users-db
spec:
  clusterIP: None
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
  selector:
    app: users-db
