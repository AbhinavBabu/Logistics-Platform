apiVersion: v1
kind: Service
metadata:
  name: notifications-service
  labels:
    app: notifications-service
spec:
  type: ClusterIP
  ports:
  - port: 3005
    targetPort: 3005
    protocol: TCP
  selector:
    app: notifications-service
