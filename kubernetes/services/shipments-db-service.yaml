apiVersion: v1
kind: Service
metadata:
  name: shipments-db
  labels:
    app: shipments-db
spec:
  clusterIP: None
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
  selector:
    app: shipments-db
